
/* ClarityJSON object; Tobe filled up with data */

$(document).ready(function() {
	
	//$.browser.msie !=undefined && 
	if($.browser.msie != true) {
		//document.write("Sorry!! Currently we are not supported in this browser. Please use Internet Explorer instead.");
		
		return false;
	}
});
